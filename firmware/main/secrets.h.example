#pragma once
// Copy this file to `secrets.h` and fill in your values.
// `secrets.h` is gitignored.

#define WIFI_SSID        "your-wifi-ssid"
#define WIFI_PASSWORD    "your-wifi-password"

// The deployed Firebase function URL, e.g.
//   https://talk-abc123-uc.a.run.app
// (printed by `firebase deploy`)
#define BACKEND_URL      "https://REPLACE-ME.a.run.app"

// Must match the DEVICE_KEY secret set on the backend.
#define BACKEND_API_KEY  "replace-with-a-long-random-string"
